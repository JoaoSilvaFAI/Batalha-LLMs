version: '3.8'

services:
  app:
    build: .
    ports:
      - "8501:8501"
      - "11434:11434"
    environment:
      - OLLAMA_BASE_URL=http://localhost:11434
    volumes:
      - .:/app
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
